{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    {% block nav %}
        {% include 'admin/_menu.admin.html.twig' %}
        {% endblock %}
    <div class="border-bottom">
    <h1 class="text-center mt-4 mb-4 text-black">Modification de l'article</h1>
    </div>

    {{ include('admin_article/_form.html.twig', {'button_label': 'Modifier'}) }}
    <div class="text-center">
    <a href="{{ path('app_admin_article_index') }}" class="btn btn-outline-primary">Retour à la liste</a>
    </div>
    {{ include('admin_article/_delete_form.html.twig') }}
{% endblock %}
