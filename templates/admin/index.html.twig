{% extends 'base.html.twig' %}

{% block title %}{{parent()}}Administration{% endblock %}

{% block body %}
    {% block nav %}
        {% include 'admin/_menu.admin.html.twig' %}
    {% endblock %}

    <div class="border-bottom">
    <h1 class="text-center mt-5  text-dark mb-4 ">{{ title }} </h1>
    </div>

    <div class="container px-4 py-5">

        <div class="row g-4 py-5 row-cols-1 row-cols-lg-2">
            <div class="col d-flex align-items-start">

                <div>
                    <h3 class="fs-2 text-dark ">Administration des articles</h3>
                    <p>Gestion des articles uniquement par un administrateur.</p>
                    <a href="{{ path('app_admin_article_index') }}" class="btn btn-primary">
                        Gérer les articles
                    </a>
                </div>
            </div>


        </div>
    </div>

{% endblock %}
